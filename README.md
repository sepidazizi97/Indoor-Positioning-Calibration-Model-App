# Indoor-Positioning-Calibration-Model-App

It is a machine learning-based indoor positioning calibration platform designed to improve low-cost trajectory tracking accuracy.

The application applies:
- Support Vector Machine (SVM)
- Random Forest (RF)
- Gradient Boosting / XGBoost
- Gaussian Process Regression (GPR)

to model spatial residuals and reconstruct calibrated indoor movement trajectories.
